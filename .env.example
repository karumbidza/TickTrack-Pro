# Database
DATABASE_URL="postgresql://username:password@localhost:5432/ticktrack_pro?schema=public"

# Clerk Auth
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="your-clerk-publishable-key"
CLERK_SECRET_KEY="your-clerk-secret-key"
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
CLERK_WEBHOOK_SECRET="whsec_your-webhook-secret"

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Google OAuth (optional)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Twilio SMS/WhatsApp (https://console.twilio.com)
TWILIO_ACCOUNT_SID="your-twilio-account-sid"
TWILIO_AUTH_TOKEN="your-twilio-auth-token"
TWILIO_PHONE_NUMBER="+1234567890"
TWILIO_WHATSAPP_NUMBER="+1234567890"

# Email (Nodemailer SMTP)
# For Gmail: Use App Password, not your regular password
# Enable 2FA on your Google account, then go to:
# https://myaccount.google.com/apppasswords to generate one
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-gmail-app-password"
EMAIL_FROM="TickTrack Pro <your-email@gmail.com>"

# File Upload (UploadThing)
UPLOADTHING_SECRET="your-uploadthing-secret"
UPLOADTHING_APP_ID="your-uploadthing-app-id"

# Paynow (Primary payment processor for Zimbabwe)
PAYNOW_INTEGRATION_ID="your-paynow-integration-id"
PAYNOW_INTEGRATION_KEY="your-paynow-integration-key"
PAYNOW_RETURN_URL="http://localhost:3000/billing/payment/return"
PAYNOW_RESULT_URL="http://localhost:3000/api/payments/paynow/webhook"

# Stripe (Alternative payment processor)
STRIPE_SECRET_KEY="sk_test_your-stripe-secret-key"
STRIPE_WEBHOOK_SECRET="whsec_your-webhook-secret"
STRIPE_PUBLISHABLE_KEY="pk_test_your-publishable-key"
# Pulse command center adapter (cross-product admin API at /api/admin/*).
# Pulse sends this as `Authorization: Bearer $ADMIN_ADAPTER_SECRET`; the routes
# refuse every request when unset. Must match PULSE_TICKTRACK_ADAPTER_SECRET in
# Pulse. Generate: openssl rand -hex 32
ADMIN_ADAPTER_SECRET=""
